<div class="content-area">
  <div class="post">
    <!-- Post Content -->
    <div class="container">
      <article>
        <!-- Title date & tags -->
        <div class="post-header">
          <h1 class="entry-title">
            <%- page.title || theme.default_post_title || "Untitled" %>
              <% if (page.subtitle && page.subtitle.length) { %>
                <p>
                  <%- page.subtitle %>
                </p>
                <% } %>
          </h1>
          <p class="posted-on">
            <%= page.date.format(config.date_format) %>
          </p>
          <div class="tags-links">
            <% if (page.tags) { %>
              <% page.tags.each(function(tag){ %>
                <a href="<%= config.root %>tags/<%= tag.name %>/" rel="tag">
                  <%- tag.name %>
                </a>
                <% }); %>
                  <% } %>
          </div>
        </div>
        <!-- Post Main Content -->
        <div class="entry-content">
          <%- page.content %>
        </div>

        <div style="text-align:center;border-top: 1px solid #ccc;margin-top: 2em;">
          <p>开通2年，未推送一篇文章，现开启全新旅程 </p>
          <p>或许更新的时间不频繁</p>
          <p>但每一次的推送都能带给你真心实意</p>
          <p>扫一扫</p>
          <p>有你想要的</p>
          <img src="https://oss.wengwang.me/images/ww1o01_qrcode_300px.png" alt="ww1o01">
        </div>

      </article>
    </div>
    <!-- Comments -->
    <div class="container">
      <%- partial('_partial/comment', {
          shortname: config.changyan_shortname
        }) %>
    </div>
    <!-- Pre or Next -->
    <div class="nav-links">
      <% if (page.prev) { %>
        <div class="nav-previous">
          <a href="<%- config.root %><%- page.prev.path %>" rel="prev"><span class="meta-arraw meta-arraw-left"></span> <%- __('prev')%></a>
        </div>
        <% } %>
          <% if (page.next) { %>
            <div class="nav-next">
              <a href="<%- config.root %><%- page.next.path %>" rel="prev">
                <%- __('next')%> <span class="meta-arraw meta-arraw-right"></span></a>
            </div>
            <% } %>
    </div>

  </div>
</div>